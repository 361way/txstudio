server:
  port: 8080
  mode: debug

database:
  path: ./data/txstudio.db

crypto:
  key_file: ./data/secret.key

cache:
  path: ./data/cache

logging:
  path: ./data/logs/txstudio.log
  max_size_mb: 20
  max_backups: 10
